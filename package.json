{
  "name": "expressdemo",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "bin": "dist/index.js",
  "type": "module",
  "scripts": {
    "ts:dev": "ts-node src/index.ts",
    "tsc:dev": "node dist/index.js",
    "tsc:build": "tsc",

    "vite:dev": "vite",
    "vite:build": "vite build",

    "esbuild:dev": "esbuild --watch",
    "esbuild:build": "node esbuild.js",

    "sea:build": "npm run tsc:build && node --experimental-sea-config sea-config.json",

    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "LincZero",
  "license": "MIT",
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.13.10",
    "esbuild": "^0.25.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.2",
    "vite": "^6.2.2"
  }
}
